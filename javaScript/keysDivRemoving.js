const keysDivRemoving = function () {
    const allKeydowns = document.querySelector('.all-keydowns');
    allKeydowns.style.transition = "opacity 0.8s ease-in-out";
    allKeydowns.style.opacity = "0";
};